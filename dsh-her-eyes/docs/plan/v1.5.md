# v1.5 — Tab + 模块开关 + 批量操作 + 供应商预设 (2026-08)

> [Back to root AGENTS.md](..) · [v1.4-1.6 ↗](./v1.4-1.6.md) · [v1.7 ↗](./v1.7.md) · [Changelog ↗](../changelog/changelog.md)

| 内容 | 要点 |
|---|---|
| **注入双条件** | `vlmEnabled` / `imggenEnabled`（默认 true/false），全链路透传 normalizeConfig/masked/applyPatch；开关关闭仅 unregister 工具，配置不变 |
| **PROVIDERS 预设表**（index.js 顶部） | custom/ollama + 28 个内置固定供应商（pi-ai catalog；国际无后缀、国内 `-cn` 后缀）。v1.5.1：normalizeConfig 不再覆写存储值；endpoint/protocol 由 effectiveCard() 在请求时推导 → 供应商 custom↔固定来回切换不丢原 endpoint（修复 401） |
| **mergePatch 碰撞坑** | `queueSave({patchCard})` 会合并同键覆盖 → 多字段联动（供应商切换等）必须走 `commitStructure({apis: 全量})` |
| **UI** | `.vlm-tabs`、`.vlm-switch` (34×18)、`.vlm-batch-btn` (32×32 chevron)、单卡确认两次点击、**.vlm-provider-dropdown** 自定义下拉非原生 select、模型下拉 ddRef 命令式测距向上翻转 |
| **主题色修复** | harness 用 `--dsw-*` token（非 --dsh-*），插件所有 `var(--dsh-*, fallback)` 永远用兜底值；新增 `@supports (light-dark())` 利用 harness `colorScheme` |
