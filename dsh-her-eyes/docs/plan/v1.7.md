# v1.7 — VLM 开关控制图片上传 + llm twin 路由 (2026-08)

> [Back to root AGENTS.md](..) · [v1.5 ↗](./v1.5.md) · [v1.8 ↗](./v1.8-autovision-lite.md) · [Changelog ↗](../changelog/changelog.md)

| 组件 | 位置/要点 |
|---|---|
| **inject** | `'llm'` → `['tools','webServer','llm']`；harness prompt admission 仅在选中模型 `inputModalities` 含 `image` 时放行图片 |
| **twin 路由** | `<provider>-her-eyes` per provider；镜像整个源目录但声明 `inputModalities:['text','image']` |
| **syncTwins(L1180)** | 与 analyze_image 同一 shouldVlm 闸门；排除 `-her-eyes`(自递归) / `-vision`(dsh-vision-router共存防护)；接入 adapters-updated 热更新 |
| **wrapper stream(L1142)** | 深度改写图片块→中文 marker(带 attachment id) → yield* ctx.llm.stream({..., provider, messages: rewritten})；preserveImageInput 保护原生多模态直看不走 marker |
| **analyze_image(L800)** | 加可选 `attachment_id` 参数：events 扫描 → collectAttachmentRefs → readImage → sniffMediaType(magic bytes PNG/JPEG/WEBP/GIF)；`image_path` 流不变；两者同给时 attachment_id 优先 |
| **测试** | 4 单元文件共 26 测试；E2E 2 个（msedge, VLM on→3 Auto Vision entries; off→0 + no file input）；junction `node_modules/@deepseek-ai`→dsh CLI node_modules |
| **禁止项** | 无 stealth/vendor/harness 改动 imggen/package.json/cordis.patch.yml README；不设 npm 依赖；不改 output.schema |

核心踩坑：cordis waterfall handler 必须 RETURN config（不是 undefined）。可选 param 必须省略 `required` 键。
